name: Deploy development

on:
  push:
    branches:
      - feature/20240928_events_endpoint_v2

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    name: Deploy
    with:
      environment: development
      app-name: relaybox
      service-name: uws
      service-suffix: UGkqsrIpIdea
      cluster-suffix: 25OYfqfJpoJz
    secrets: inherit
