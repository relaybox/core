name: Deploy development

on:
  push:
    branches:
      - main
      - feature/20240919_tests__fix_paths

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    name: Deploy Application
    with:
      environment: development
      app-name: relaybox
      service-name: uws
      service-suffix: UGkqsrIpIdea
      cluster-suffix: 25OYfqfJpoJz
    secrets: inherit
