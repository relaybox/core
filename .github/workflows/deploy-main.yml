name: Deploy development

on:
  push:
    branches:
      - main
      - feature/20240919_reusable_workflows

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: development
      app-name: relaybox
      service-name: uws
      service-suffix: UGkqsrIpIdea
      cluster-suffix: 25OYfqfJpoJz
    secrets: inherit
